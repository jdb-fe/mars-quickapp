<template>
    <div class="title {{hideBorder ? '' : 'border'}}" style="opacity: {{opacity || 0}}">
        <div class="nav">
            <image show="{{!!showLeft}}" class="icon" onclick="goback()" src="/common/image/left.png" />
        </div>
        <text class="text">{{title || '前端录'}}</text>
        <div class="nav"></div>
    </div>
</template>
<style lang="less" src="./index.less"></style>
<script src="./index.js"></script>